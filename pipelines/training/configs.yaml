# Configuration for Continuous Training (CT) Pipeline
ranker:
  n_estimators: 100
  learning_rate: 0.05
  num_leaves: 15
  min_child_samples: 5
  objective: lambdarank
  metric: ndcg
  eval_at: [5, 10]
